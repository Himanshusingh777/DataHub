// Quick Actions — stub. Legacy component that referenced old routes (CONNECTORS_NEW,
// PIPELINES_NEW, LOGS) which no longer exist. Not imported anywhere.
// Kept as a null export to avoid future import errors.
export function QuickActions() {
  return null;
}
