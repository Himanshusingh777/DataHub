// Recent Connectors — stub. Legacy component that referenced old routes (CONNECTORS_NEW)
// which no longer exist. Not imported anywhere.
// Kept as a null export to avoid future import errors.
export function RecentConnectors() {
  return null;
}
